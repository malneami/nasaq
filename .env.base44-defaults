# Base44 development defaults.
# Real secrets (Supabase, Anthropic) are delivered via /run/base44/app.env
# which is listed AFTER this file in compose, so it always wins.
ANTHROPIC_MODEL=claude-sonnet-4-5
